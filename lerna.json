{
  "packages": ["packages/*"],
  "npmClient": "npm",
  "version": "independent",
  "command": {
    "publish": {
      "allowBranch": "trunk",
      "ignoreChanges": [
        "**/.eslintrc",
        "**/.gitignore",
        "**/*.md",
        "**/test/**"
      ]
    },
    "version": {
      "message": "chore(release): publish packages"
    }
  }
}
